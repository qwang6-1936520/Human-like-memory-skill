# OpenClaw Compatibility Artifacts

This directory preserves legacy OpenClaw publishing metadata from the previous package:

- `skill.json`
- `_meta.json`

Hermes publishing uses `SKILL.md` at repository root as the authoritative manifest.
